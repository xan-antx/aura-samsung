FROM python:3.12-slim

WORKDIR /app

COPY requirements-live.txt .
RUN pip install --no-cache-dir -r requirements-live.txt

# the core stays stdlib; only the live adapter needs the dependency above
COPY agent.py perception.py harness.py live_server.py ./

EXPOSE 8765

CMD ["python", "live_server.py"]
